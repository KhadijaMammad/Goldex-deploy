// Stub types for `babel__traverse` to satisfy TS when @types isn't installed.
// Replace with `npm install -D @types/babel__traverse` if available.

declare module 'babel__traverse' {
  const _default: any;
  export default _default;
}
